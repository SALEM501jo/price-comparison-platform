fastapi==0.141.1
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# JWT. PyJWT, not python-jose: the latter's last release was 2021 and it
# carries five advisories including PYSEC-2025-185, which has no fix. It also
# pulled in `ecdsa`, itself unfixed. This module signs every token we issue.
PyJWT[crypto]==2.13.0

# Password hashing. bcrypt is used directly -- passlib was declared but never
# imported, which meant the real dependency was only present transitively and
# would have vanished the moment passlib was removed.
bcrypt==4.2.0

httpx==0.27.2
redis==5.1.0
python-multipart==0.0.32
