# Backend API URL.
# Copy this file to .env.local and adjust if your backend is not on port 8000.
# Only VITE_-prefixed variables reach the browser bundle -- never put secrets here.
VITE_API_URL=http://localhost:8000
